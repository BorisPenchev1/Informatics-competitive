#include<bits/stdc++.h>

using namespace std ;

const int maxn = 1e6 + 2;

int main(){

    int n ; cin >> n ;
    int a[maxn] ;

    for (int i = 0 ; i < n ; i++){
        cin >> a[i] ;
    }

    for (int i = 0 ; i < n ; i++){
        while (a[i] < a[i + 1])
    }

    return 0 ;
}