#include<bits/stdc++.h>

using namespace std;

int nums1 = [100], nums2 = [100], nums = [100], n1, n2, i, j, lenght, curr_i, check_i;

int main(){
	
	cin >> n1 >> n2 ;
	length = sizeof(nums) / sizeof(nums[0]);
	
	for (i = 0 ; i < n1 ; i++){
		cin >> nums1[i] ;
	}
	
	for (j = 0 ; j < n2 ; j++){
		cin >> nums2[j] ;
	}
	
	nums[] = nums1[] + nums2[];
	
	for (x = 0 ; x < lenght ; x++){
		if (curr_index == check_i) cur_i++;
		
		if (nums[x] == nums[(n-1)-x]){
			nums[].remove(curr_i) ;
		}
	}
	
	return 0;
}
